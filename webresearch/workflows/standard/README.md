# Standard Workflow

The standard workflow is the baseline research flow:

1. Plan the research.
2. Run official, recent, and broad research lanes in parallel.
3. Review coverage and conflicts.
4. Run gap research when the reviewer reports critical gaps.
5. Produce the final answer and structured output.

Runtime orchestration lives in `workflow.py`. Standard-specific settings live in `config.py`.
