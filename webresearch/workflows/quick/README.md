# Quick Workflow

The quick workflow is the lean research flow:

1. Plan the research.
2. Run official and broad research lanes in parallel.
3. Produce the final answer and structured output.

It intentionally disables the recent research lane, reviewer, and gap loop.

Runtime orchestration lives in `workflow.py`. Quick-specific settings live in `config.py`.
